# Tasks Backend

Use docker Compose file to launch a MariaDB. Then you can launch the application. If you want to use a Docker Container for the Springboot application you need to add the container to the network of the MariaDB or create a new Network with the MariaDB container and the Springboot application.
